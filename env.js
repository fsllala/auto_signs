const PUSH_URL = "http://www.pushplus.plus/send"; // pushplus
const PUSH_PLUS_TOKEN = process.env.PUSH_PLUS_TOKEN;

// ikun
const USERNAME = process.env.USER;
const PASSWORD = process.env.PWD;

module.exports = {
  PUSH_URL,
  PUSH_PLUS_TOKEN,
  USERNAME,
  PASSWORD,
};
